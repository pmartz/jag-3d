SET(EXE_NAME SbMem )
SET(IS_WINDOWED_APP FALSE)
SET( CATEGORY Test )

SET(EXE_COMMON_FILES
	${CMAKE_CURRENT_SOURCE_DIR}/t.cpp
)

#SET_EXECUTABLE_INCLUDE_DEPENDENCIES(${OPENGL_INCLUDE_DIR})

CREATE_EXECUTABLE(${EXE_NAME} ${IS_WINDOWED_APP} ${EXE_COMMON_FILES})

SET_INTERNAL_EXECUTABLE_DEPENDENCIES(${EXE_NAME} iiMem) 
#SET_EXTERNAL_EXECUTABLE_DEPENDENCIES(${EXE_NAME} ${OPENGL_LIBRARIES})


