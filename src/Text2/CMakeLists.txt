SET(LIB_NAME iiText2)
SET(HEADER_PATH_SUFFIX "Text2") 

SET(HEADER_PATH "${Chaskii_SOURCE_DIR}/include/${CHASKII_HEADER_PATH_BASE}/${HEADER_PATH_SUFFIX}")

SET(LIB_PUBLIC_HEADERS
     ${HEADER_PATH}/PixelBuffer.h
     ${HEADER_PATH}/Text2D.h
     ${HEADER_PATH}/FTLibrary.h
)

SET(LIB_COMMON_FILES
	${CMAKE_CURRENT_SOURCE_DIR}/PixelBuffer.cpp
	${CMAKE_CURRENT_SOURCE_DIR}/Text2D.cpp
	${CMAKE_CURRENT_SOURCE_DIR}/DefaultFont.cpp
)

SET_EXTERNAL_INCLUDE_DEPENDENCIES(${FREETYPE_INCLUDE_DIRS} ${ICONV_INCLUDE_DIR})

# Quotes are needed to prevent CMake confusion for variable arguments with lists
CREATE_LIBRARY("${LIB_NAME}" "${LIB_PUBLIC_HEADERS}" "${LIB_COMMON_FILES}")

SET_INTERNAL_LIBRARY_DEPENDENCIES(${LIB_NAME} iiMem )
SET_EXTERNAL_LIBRARY_DEPENDENCIES(${LIB_NAME} ${FREETYPE_LIBRARY} ${OPENGL_LIBRARIES} ${ICONV_LIBRARY})

CONFIGURE_PUBLIC_HEADER_INSTALL_PATH(${HEADER_PATH_SUFFIX} ${LIB_PUBLIC_HEADERS})


